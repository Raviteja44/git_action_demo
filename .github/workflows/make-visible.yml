name: Make Workflows Visible

on:
  push:
    branches-ignore:
      - main
      - prod
      - uat

jobs:
  make-visible:
    runs-on: ubuntu-latest
    steps:
      - name: Wake up GitHub Actions for this branch
        run: echo "Workflows are now visible for branch: $GITHUB_REF_NAME"
